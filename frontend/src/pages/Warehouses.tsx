import { Typography } from 'antd';

const { Title } = Typography;

const Warehouses = () => {
  return (
    <div>
      <Title level={2}>仓库管理</Title>
      <p>仓库管理页面正在开发中...</p>
    </div>
  );
};

export default Warehouses;